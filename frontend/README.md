# FullStack (VULNERABLE DEMO)

**WARNING**: This project is intentionally insecure. Use only in a controlled lab.

## Run (local / lab)
1. Build and run everything:
   ```bash
   docker-compose up --build
